fn main() {
    eprintln!("This is a stub package. Install the actual CLI via:");
    eprintln!();
    eprintln!("  cargo binstall voice-bird-cli");
    eprintln!();
    eprintln!("Or download directly from:");
    eprintln!("  https://github.com/RekunDzmitry/voice-bird-releases/releases");
}
